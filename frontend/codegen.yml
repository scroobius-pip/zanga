overwrite: true
schema: "https://zanga-api.now.sh/graphql"
documents: "./**/*.graphql"
generates:
  ./generated/graphql.ts:
    plugins:
      - "typescript"
      - "typescript-operations"
      # - "typescript-graphql-files-modules"
      - "typescript-graphql-request"

